{
  "scripts": {
    "build:sass": "node-sass -r css-source -o css-output",
    "watch:sass": "npm run build:sass && npm run build:sass -- -w"
  },
  "devDependencies": {
    "node-sass": "^7.0.1"
  }
}
